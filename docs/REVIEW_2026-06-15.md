# SHELF — CODEBASE + UI REVIEW (2026-06-15)

**Reviewer:** Claude (Opus 4.8, 1M ctx), multi-agent orchestrated review.
**Live instance:** systemd `shelf.service` on `:8000` (healthy at start).
**Scope decisions (operator):** Full live scan (may trigger real external crawls); Exhaustive/max depth.
**Method:** Stage-0 inline recon (this section) → Stage-1 parallel deep-dive subagents per focus
area → Stage-2 adversarial multi-vote verification + dedup → this document → Stage-3 coordinated
fix plan (no code changes until approved).

> **Status:** ALL STAGES COMPLETE (2026-06-15). Stage 0 baseline, Stage 1+2 via workflow
> `wf_162cfe16-3c0` (31 confirmed findings: 1 critical / 13 high / 17 medium; + 61 lows + 28
> rejected), Stage 3 coordinated fix plan below. NO code changed — awaiting approval to implement.
> Caveat: bug/security sweep hit its 3-round cap without going dry (more may remain).

---

## LEGEND
`[BUG]` real defect · `[RISK]` correctness/ops hazard to confirm · `[SEC]` security ·
`[PERF-CPU]` cpu cost · `[PERF-IO]` disk read/write cost · `[NET]` external-request usage ·
`[WIRING]` backend↔frontend mismatch · `[DEADCODE]` unused/stale · `[UX]` usability ·
`[UI]` visual/design · `[OK]` verified clean.

Each finding records: **what · severity · how found · why flagged · files reviewed · evidence.**

---

## STAGE 0 — GROUND TRUTH & BASELINE

### Architecture
- **Backend:** FastAPI/Python, ~35k LOC / 112 files, SQLite (WAL). 20 routers, large ingestion
  engine (adapters: comix, royalroad, gutenberg, standardebooks, jnovel, libgen, generic_feed,
  local_folder), integrations (prowlarr, readarr, sabnzbd, kapowarr, goodreads).
- **Frontend:** React 19 + TS, ~13k LOC / 44 files, Vite 8, Tailwind 4, react-query, zustand.
  10 pages, ~30 components, single API client (`src/api/client.ts`).
- **Deploy:** systemd unit; optional headless Chromium (Playwright) + zendriver/curl_cffi for
  Cloudflare/Turnstile challenge solving.

### Live runtime baseline (uptime ~1d3h at capture, PID 329693)
| Metric | Value | Note |
|---|---|---|
| RSS / VmPeak | 2.44 GB / 7.86 GB | + persistent headless Chromium tree |
| CPU consumed | ~14.5 h / day | high for a single-user reader |
| Threads | 18 | |
| `rchar` / `read_bytes` | **9.66 TB** / 31.5 GB | huge logical read volume |
| `wchar` / `write_bytes` | **602 GB** / 925 GB | huge write volume; `cancelled_write_bytes` 21.5 GB |
| disk_free / wal | 100 GB / 3 MB | healthy at capture |

### Database (SQLite, `backend/shelf.db`)
- **Size: 3.77 GB.** Plus `-shm` 2.8 MB, `-wal` 0.6 MB at capture.
- Largest tables (rows): `indexed_pages` 421,962 · `catalog_tags` 251,840 · `catalog_works`
  186,086 · `catalog_groups` 160,786 · FTS docsize/content/index ≈ 105,359 ea · `chapters`
  84,969 · **`chapter_contents` 84,849** (full chapter text stored in-DB — likely the bulk of
  the 3.77 GB) · `catalog_categories` 18,459 · `works` 1,430 · `request_stats` 1,318.
- **`[PERF-IO]` candidate:** three stale 3 GB DB backups in `backend/` (`.pre-libclean`,
  `.pre-relink`, `.pre-stocklink`) = ~9 GB dead disk. Flag, don't delete (not ours to assume).

### External-request surface (outbound hosts found)
- **Metadata/catalog:** googleapis.com (Google Books), openlibrary.org, covers.openlibrary.org,
  gutendex.com, hardcover.app, graphql.anilist.co.
- **Content sources:** gutenberg.org, royalroad.com, standardebooks.org, j-novel.club /
  labs.j-novel.club, comix.to / api.comix.to.
- **Acquisition:** annas-archive.gl, z-library.sk, oceanofpdf.com (libgen.py providers).
- **Integrations (operator-configured hosts):** prowlarr, readarr, sabnzbd, kapowarr, goodreads.

### Existing request-control machinery (so review judges *use*, not *existence*)
- `ingestion/fetcher.py`: **conditional requests supported** — `If-None-Match` (etag) +
  `If-Modified-Since` (last_modified) at lines 557–560; robots.txt read+cache with 3600s TTL
  (`_robots_ttl`, ~271/514); adaptive per-source throttle factor (1.0→16.0, grows on pushback
  ~201, decays on success streak ~230); Retry-After honoured.
  → **Open question for Stage 1:** do callers persist & resend etag/last_modified, or is the
  conditional-request path dead? Is HTTP response caching keyed/stored anywhere?
- Caching layers present: `cache.py`, `imagecache.py`, `covers.py`, `imgproxy.py` (disk cache).
- Telemetry: `telemetry.py` + `request_stats` table (1,318 rows) tracks outbound request volume.
- Guards: `ingestion/netguard.py` (SSRF/DNS-rebind pinning), `ingestion/blocklist.py`,
  `integrations/ratelimit.py`, `ingestion/crawl_tuning.py`, `ingestion/scheduler.py` (APScheduler).

### Prior-review dedup reference
Five large prior docs (`REVIEW_2026-06-13.txt`, `bugs review 5.txt`, `fix_implementation.txt`,
`fix_issues.txt`, `IMPLEMENTATION_REPORT.txt`) were indexed. **The vast majority of past findings
are marked RESOLVED** (security P0/P1, concurrency races, Cloudflare detection, migrations,
matching, perf, ops, frontend/PWA). The new review must NOT re-report those as new.

**Genuinely OPEN (accepted/deferred):** author-mismatch penalty == MATCH_FLOOR (release_matcher
~241, deliberate); identity-key union no author safety net (catalog.py ~381, deliberate); 8 dead
imports + `works.py:95` stale `fetched` param (ruff --fix never run); libgen `candidates_for` too
strict + dead zlibrary/oceanofpdf render fallbacks (RC#4); cascade early-abort (RC#3d); stocking
funnel observability; alembic-vs-boot schema duality (`db.py:63-422`, only PARTIAL); pervasive
sync-DB-in-async-routes (only partly converted); dependency pinning / CVE scan / robots Crawl-delay
enforcement — explicitly never audited.

**CLAIMED-FIXED — re-verify (highest priority, self-attested by same author, green suite that
"asserts buggy behavior"):** backup zip-slip RCE path containment (backup.py ~657); CrawlJob
two-writer lease race (scheduler.py:455-563); `sweep_orphan_staging` shared-drop-zone delete /
data-loss blast radius (downloads.py `_staging_root`); comix Turnstile crawler (env-dependent);
comic-cover durable heal (~77k slow); the whole bugs-review-5 catalog (8 HIGH/14 MED/17 LOW);
Section 13/14 matching changes; async-route DB blocking closure.

---

## STAGE 1 — DEEP-DIVE COVERAGE (multi-agent workflow `wf_162cfe16-3c0`)

124 agents, ~4.81M tokens, 9 focus-area finders → adversarial multi-vote verification
(3 diverse-lens skeptics for critical/high, 1 for medium; majority-real gating) → bounded
loop-until-dry sweep for bugs+security.

| Round | Result |
|---|---|
| Round 1 (9 finders) | 75 raw findings, 17 confirmed on first verify pass |
| Sweep round 1 | 16 fresh → 4 confirmed |
| Sweep round 2 | 13 fresh → 4 confirmed |
| Sweep round 3 | 16 fresh → 6 confirmed |

**Totals:** 120 findings → **31 confirmed** · 28 rejected (false positives killed by verification)
· 61 unverified-low (kept, single-pass). Confirmed mix: **1 critical, 13 high, 17 medium**.

**Coverage caveats (no silent gaps):**
- The sweep hit its **MAX_SWEEP_ROUNDS=3 cap without going dry** (round 3 still found 6 new
  confirmed bugs/security) → additional latent bugs/security issues likely remain. A further
  sweep is warranted if you want deeper coverage.
- 2 individual verification votes failed to emit structured output (absorbed by majority voting;
  every focus area still produced confirmed findings, so no area was lost).

---

## STAGE 2 — VERIFIED & DEDUPED FINDINGS

31 confirmed below. **3 are positive reality-check confirmations (no action):** F02 (backup
zip-slip containment holds), F03 (CrawlJob lease race closed), F16 (comic-cover heal progressing).
**Near-duplicate pairs to merge for fixing** (same root cause, surfaced by both round-1 and sweep):
- F09 + F25 — `hooked_work_id` lost on import rollback (downloads.py + libgen.py)
- F10 + F23 — `_apply_series` lost on import rollback (downloads.py + libgen.py)
- F12 + F29 — metadata-provider redirect SSRF (no per-hop revalidation)
- F13 + F28 — apprise/ntfy notification-channel SSRF
- F19 + F27 — upload / zip-bomb size cap (HTTP-body level + decompression level)

→ **~22 distinct actionable work items** (see Stage 3).

#### F01 · [PERF-IO] Catalog regroup full-rebuilds 600k+ rows every 10 min for tiny delta
- **Severity:** CRITICAL · **Area:** perf · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/catalog_groups.py:193, /root/Shelf/backend/app/ingestion/catalog_groups.py:240, /root/Shelf/backend/app/ingestion/catalog_groups.py:262, /root/Shelf/backend/app/ingestion/scheduler.py:1460`
- **How found:** py-spy live dump caught regroup_catalog active; journal shows it runs every ~10min (36x/6h); measured table sizes + rewrite pattern in catalog_groups.py:240-263
- **Why flagged:** Each catalog_regroup_tick DELETEs+re-INSERTs catalog_tags (263k rows) and catalog_groups (161k rows), then UPDATEs catalog_works.group_id to NULL and back across 187k rows. That is ~611k row writes plus all secondary-index updates (catalog_tags(kind,slug); catalog_groups x4 indexes; catalog_works.group_id) PER RUN, ~144 runs/day = ~88M row writes/day. The watermark (_should_regroup) flips on every count/id-sum change, so it fires continuously while the crawl adds rows (186,676 -> 187,068 over the sampled 6h = only ~400 new rows, yet the whole table was rewritten 36 times). This full-rebuild-for-incremental-delta against a multi-GB DB, multiplied by WAL+checkpoint amplification (1.0 TB block …
- **Evidence:** DELETE FROM catalog_tags; DELETE FROM catalog_groups; UPDATE catalog_works SET group_id = NULL; then _executemany INSERT 161k groups + 263k tags + UPDATE group_id on 187k works. journal: 'catalog regroup: rows=187068 groups=161248 tags=263372' every ~10min. Table counts: catalog_tags=263,372 catalog_groups=161,248 catalog_works=187,078.
- **Fix direction (not applied):** Make regroup incremental (only re-cluster buckets touched by changed rows) or gate on a minimum row-count delta so a 0.2% change does not rewrite 100% of the grouping.

#### F02 · [SEC] Backup zip-slip containment guards every restore media entry
- **Severity:** HIGH · **Area:** karen · **Confidence:** high · **Status:** verify-prior
- **Files:** `/root/Shelf/backend/app/backup.py:681, /root/Shelf/backend/app/backup.py:703, /root/Shelf/backend/app/backup.py:711`
- **How found:** Read backup.py _restore_media + _zip_entry_dest; grepped all file-writing paths
- **Why flagged:** Confirms the P0 RCE-via-path-traversal fix is genuinely present and is the ONLY file-writing restore path, so it cannot be bypassed.
- **Evidence:** _restore_media loops zf.infolist() and calls dest=_zip_entry_dest(root, name[len('media/'):]); _zip_entry_dest rejects absolute/drive/'..'/empty segments AND verifies dest.is_relative_to(root.resolve()), returning None (entry skipped with 'REJECTED unsafe zip entry' warning) on any escape. DB-portion entries use hardcoded names (zf.open(f'data/{tn}.jsonl')), not attacker-controlled. No extractall/extract anywhere.
- **Fix direction (not applied):** None needed — fix holds.

#### F03 · [BUG] CrawlJob two-writer lease race is fully closed
- **Severity:** HIGH · **Area:** karen · **Confidence:** high · **Status:** verify-prior
- **Files:** `/root/Shelf/backend/app/ingestion/scheduler.py:179, /root/Shelf/backend/app/ingestion/scheduler.py:259, /root/Shelf/backend/app/ingestion/scheduler.py:519, /root/Shelf/backend/app/ingestion/scheduler.py:634, /root/Shelf/backend/app/ingestion/scheduler.py:741`
- **How found:** Read scheduler.py lease stamp/renew/expire + reaper + tick claim paths
- **Why flagged:** Confirms the historical two-writer race (reaper revives a job a live runner still owns) cannot occur: every commit path is token-gated.
- **Evidence:** _process_job stamps a fresh token on claim (179); before EACH chapter commit it calls _renew_lease (259) which re-reads CrawlJob.lease_token fresh from DB and returns False (run aborts via db.rollback) if the token was bumped. Reaper only revives a 'running' job when _lease_expired (634) and bumps lease_token, making the abandoned coroutine's later commits no-op. tick() skips 'running' jobs whose lease is still live (741). Single-writer invariant holds end-to-end.
- **Fix direction (not applied):** None needed — fix holds.

#### F04 · [DEADCODE] Conditional-GET (ETag/If-Modified-Since) path is dead — no caller persists/resends validators
- **Severity:** HIGH · **Area:** net · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/fetcher.py:538, /root/Shelf/backend/app/ingestion/fetcher.py:557, /root/Shelf/backend/app/ingestion/tracker.py:185, /root/Shelf/backend/app/ingestion/engine.py:309, /root/Shelf/backend/app/models.py:1`
- **How found:** Grepped backend for etag/last_modified/If-None-Match/If-Modified-Since/304; only matches are fetcher.py's own parameter plumbing (lines 543-560). No adapter or integration passes etag=/last_modified=, nothing reads resp.headers ETag/Last-Modified, no model column stores them, no 304 branch exists.
- **Why flagged:** The fetcher advertises conditional GET as a feature (docstring line 10) and accepts etag/last_modified kwargs, but it is completely unwired. Every refresh re-downloads full bodies the origin could answer with an empty 304. Highest-value target: the ~6h refresh cadence (scheduler refresh_enqueue -> tracker.discover_updates -> adapter.discover_work()+list_chapters()), which re-fetches and re-parses each hooked serial's TOC/index page in full every cycle even when unchanged — N full TOC fetches per refresh interval for N ongoing serials. Content-checksum dedup (engine.py:309-311) only fires AFTER the full body is downloaded, so it saves storage/CPU but not the request/bytes.
- **Evidence:** fetcher.get(): `if etag: headers['If-None-Match']=etag` / `if last_modified: headers['If-Modified-Since']=last_modified` (557-560). Grep `etag=`/`last_modified=` across backend: no callers. Grep `304`: only Retry-After/location handling. models.py grep etag|last_modified: No matches found.
- **Fix direction (not applied):** Persist ETag/Last-Modified per Work TOC / per IndexedPage on fetch, pass back into fetcher.get(); treat 304 as 'unchanged, skip parse'.

#### F05 · [PERF-CPU] authors_compatible recomputes _author_norm ~15M times per regroup
- **Severity:** HIGH · **Area:** perf · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/catalog.py:441, /root/Shelf/backend/app/ingestion/extract.py:960, /root/Shelf/backend/app/ingestion/extract.py:971`
- **How found:** py-spy sampling: the 99.9% CPU thread was repeatedly in _author_norm (extract.py:967) / authors_compatible (974) under regroup; reproduced + timed the comparison count offline
- **Why flagged:** In _union_find_groups the fuzzy-merge pairwise loop calls authors_compatible(authors[i], authors[j]) for every candidate pair, and authors_compatible calls _author_norm (NFKD normalize + combining-char strip + Unicode regex sub) on BOTH authors every call with NO caching. Measured 7,504,377 pairwise comparisons => ~15,008,754 _author_norm calls per regroup, ~34s of pure CPU per run. At the 10-min regroup cadence that is ~34s x 144 = ~82 min CPU/day; across the 743 regroups since boot ~7h CPU spent purely re-normalizing the same ~187k author strings. The title token sets (toks) are ALREADY precomputed once per row (catalog.py:374 'precompute once (was recomputed per pair)') but the identical …
- **Evidence:** py-spy: _author_norm (app/ingestion/extract.py:967) <- authors_compatible (974) <- regroup. Timing: 200k _author_norm calls=0.451s -> 15.0M calls ~=33.9s; precomputing per unique row (187k) ~=0.4s (88x reduction). Loop: 'if inter == 0 or not authors_compatible(authors[i], authors[j])' at catalog.py:441 inside O(k^2) per-token-bucket loop.
- **Fix direction (not applied):** Precompute author_tokens = [frozenset(_author_norm(a).split()) for a in authors] once before the loop (mirroring toks) and compare token sets directly.

#### F06 · [PERF-IO] _enqueue_links loads entire 212k-URL column per page fetched
- **Severity:** HIGH · **Area:** perf · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/indexer.py:547, /root/Shelf/backend/app/ingestion/indexer.py:559`
- **How found:** py-spy caught _enqueue_links (indexer.py:550) active in fetchall(); measured set-build cost against live DB per active site
- **Why flagged:** On every page stored, _enqueue_links runs 'SELECT url FROM indexed_pages WHERE site_id=X' and materializes ALL of that site's URLs into a Python set. Site 6 has 212,034 pages -> a ~10 MB / 212k-element set built (~0.16s CPU + GC churn) for EACH page processed; with 3 active sites x parallel_fetches=4 per ~10s tick that is up to ~12 full-column reads (~1.5M URL rows) every 10s while crawling. This re-reads the same data indefinitely and scales with site size, contributing materially to the 10.5 TB logical-read / 2.6B read-syscall baseline. The set is used only to test membership of a handful of freshly-discovered candidate URLs.
- **Evidence:** existing = {u for (u,) in db.execute(select(IndexedPage.url).where(IndexedPage.site_id == site.id)).all()}. Live: site 6 set of 212,034 urls built in 0.164s; site 7 84,388 in 0.168s. indexed_pages total=425,272.
- **Fix direction (not applied):** Replace the full-column load with a bounded existence query on just the candidate URLs (SELECT url ... WHERE site_id=X AND url IN (:candidates)), served by sqlite_autoindex_indexed_pages_1(site_id,url).

#### F07 · [PERF-IO] Cover-cache tick does 8 full-table scans every 30s for ~11 rows
- **Severity:** HIGH · **Area:** perf · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/scheduler.py:943, /root/Shelf/backend/app/ingestion/scheduler.py:959, /root/Shelf/backend/app/ingestion/scheduler.py:1424`
- **How found:** EXPLAIN QUERY PLAN on the _cache_covers_batch queries (all 'SCAN'), plus counts of remaining remote/imgcache covers
- **Why flagged:** _cache_covers_batch runs every 30s (cache_images job) and issues 8 cover_url LIKE queries across catalog_groups/catalog_works/works/indexed_pages: 4x LIKE '%/imgcache/%' (salvage) + 4x LIKE 'http%' (localize). There is NO index on cover_url on any of these tables, so every query is a full B-tree SCAN. indexed_pages alone is 425k rows whose pages carry ~1.3 GB of html/text blobs. The work is almost entirely wasted: only 11 remote http covers remain (catalog_groups 1, catalog_works 1, works 0, indexed_pages 9), yet the imgcache-salvage LIKE still scans 76k+98k+122k localized rows every 30s. ~1.5M row scans/30s = ~4.3M/min = ~7B row-scans/day, a large slice of the 9.66 TB/day logical reads + 2.…
- **Evidence:** EXPLAIN: every cover_url LIKE query => 'SCAN <table>' (no index). Remaining remote covers: catalog_groups 1, catalog_works 1, works 0, indexed_pages 9. imgcache-localized rows scanned each pass: catalog_groups 76,259 / catalog_works 97,694 / indexed_pages 122,027. Scheduler: sched.add_job(cache_images_tick, 'interval', seconds=30 ...).
- **Fix direction (not applied):** Add a partial index on cover_url (WHERE cover_url LIKE 'http%') or track a 'needs-localization' flag, and back off the 30s cadence sharply once the remote backlog is empty.

#### F08 · [SEC] imagecache image fetch lacks DNS-rebind IP pinning (SSRF)
- **Severity:** HIGH · **Area:** security-audit · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/imagecache.py:196, /root/Shelf/backend/app/imagecache.py:204, /root/Shelf/backend/app/imagecache.py:43, /root/Shelf/backend/app/routers/imgproxy.py:57, /root/Shelf/backend/app/ingestion/netguard.py:85`
- **How found:** Traced /api/cover -> imagecache.cache_image -> _fetch_image; compared SSRF handling against netguard.safe_get which pins to the validated IP (S6 fix).
- **Why flagged:** _fetch_image validates with assert_public_url(url) (resolves the host, checks all IPs are public) but then connects with _get_client().get(url) using the HOSTNAME, re-resolving DNS at connect time. A domain whose DNS alternates between a public IP (passes the check) and an internal IP (169.254.169.254 / RFC-1918, returned at connect) defeats the guard — a classic TOCTOU DNS-rebinding SSRF. The /api/cover endpoint accepts arbitrary attacker-controlled http(s) URLs from ANY authenticated user (no allowlist, unlike /api/img), so internal services/cloud-metadata can be reached and the response body returned. The prior S6 fix pinned the connection to the validated IP ONLY in netguard.safe_get + f…
- **Evidence:** imagecache._fetch_image: `assert_public_url(url)` (l196) then `r = _get_client().get(url, headers=headers)` (l204) — no IP pin. Contrast netguard.safe_get l117-118: `pinned_url, host_header, ext = _pin_to_ip(cur, ips[0]); r = client.get(pinned_url, headers=host_header, extensions=ext)`. /api/cover (imgproxy l71-82) passes any http(s) `u` straight to cache_image with NO allowlist (only /api/img gates on referer_for()).
- **Fix direction (not applied):** Route imagecache fetches through netguard.safe_get (or replicate _pin_to_ip: connect to the validated IP, carry Host header + sni_hostname) so each hop connects to the exact IP just validated.

#### F09 · [BUG] hooked_work_id set on cw BEFORE add_to_library, then rolled back on failure — catalog hook lost
- **Severity:** HIGH · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/downloads.py:728-748, /root/Shelf/backend/app/ingestion/libgen.py:914-926`
- **How found:** Reading _import_completed lines 728-748 in downloads.py
- **Why flagged:** At line 733-734, cw.hooked_work_id is set unconditionally (if None). Then add_to_library is called and if it throws, the except block does db.rollback() (line 740). The rollback undoes the committed job/work rows AND the cw.hooked_work_id assignment — but the local `cw` object in memory still has hooked_work_id set. The recovery block at lines 741-747 then re-sets job fields and does db.commit() — but does NOT re-set cw.hooked_work_id. Depending on SQLAlchemy's session state after rollback, either the cw assignment is lost in DB (catalog not hooked — the work is imported but catalog stays un-hooked) or the dirty state is re-committed without the rest of the import transaction. The same patte…
- **Evidence:** if cw is not None and cw.hooked_work_id is None:     cw.hooked_work_id = work.id  # line 733-734 ... except Exception:  # BLE001     db.rollback()  # rolls back hooked_work_id too     job.work_id = work.id  # line 742 — re-sets job fields     # cw.hooked_work_id NOT re-set here
- **Fix direction (not applied):** Move cw.hooked_work_id = work.id into the recovery block along with the other re-set fields, or set it after the try/except block that wraps add_to_library.

#### F10 · [BUG] _apply_series mutations lost on add_to_library rollback in _import_completed
- **Severity:** HIGH · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `backend/app/ingestion/downloads.py:735-748`
- **How found:** Code trace: downloads.py _import_completed() order of operations
- **Why flagged:** _apply_series(work, cw) at line 735 writes work.series and work.series_position to the ORM. Then add_to_library is called inside a try/except; on exception, db.rollback() at line 740 discards the series mutations along with the failed library placement. Lines 742-747 then re-set job.work_id/verified/status/completed_at and cw.hooked_work_id but do NOT re-call _apply_series. The final db.commit() at line 748 persists the job fields but the work's series tag is permanently lost — the library card will never show a series or series position for a book where add_to_library happened to fail.
- **Evidence:** 735: _apply_series(work, cw) 736: if job.user_id: 737:     try: 738:         add_to_library(db, job.user_id, work.id, shelf_id=job.target_shelf_id) 739:     except Exception: 740:         db.rollback()  # ← discards _apply_series mutations 741:         job.work_id = work.id  # re-applied 742:         ... 747: # _apply_series NOT re-called 748: db.commit()
- **Fix direction (not applied):** Move _apply_series(work, cw) to AFTER the db.rollback() recovery block, or re-call it in the except block before db.commit().

#### F11 · [SEC] Path traversal in EPUB/CBZ export reads arbitrary server files
- **Severity:** HIGH · **Area:** sweep-sec · **Confidence:** high · **Status:** new
- **Files:** `backend/app/epub_export.py:26-40, backend/app/epub_export.py:174-176, backend/app/routers/delivery.py:101-108, backend/app/routers/delivery.py:134-135, backend/app/sanitize.py:84-91, backend/app/imagecache.py:307-324`
- **How found:** Traced chapter-image resolution from download/export/send-to-kindle endpoints into epub_export._local_image_bytes; confirmed sanitizer preserves '/media/..' img srcs and verified a live read of /etc/hostname through the exact code path.
- **Why flagged:** _local_image_bytes() resolves a chapter <img src> beginning with '/media/' as `media_dir() / src[len('/media/'):]` with NO containment/realpath check, then read_bytes() and embeds the result into the EPUB/CBZ the requester downloads. A src like '/media/../../../../etc/passwd' escapes media_dir and discloses any file readable by the service user (running as root here). The sibling '/covers/' branch is hardened with os.path.basename — the '/media/' branch is the gap, confirming the omission is an oversight. The sanitizer (sanitize.py:87) explicitly allows site-relative srcs and only blocks javascript:/data:/vbscript: schemes, so the traversal src survives ingest; localize_html_images only rewr…
- **Evidence:** Live: media_dir=/root/Shelf/backend/media ; src='/media/../../../../../../etc/hostname' -> read_bytes()=b'WEB\n' (read /etc/hostname). sanitize_html keeps `<img ... src="/media/../../../../../../etc/hostname"/>`; extract_image_srcs returns that src. _local_image_bytes: `path = media_dir() / src[len('/media/'):]` then `return path.read_bytes(), ...` with no `os.path.realpath`/`is_relative_to` guard, whereas the /covers/ branch uses `covers_dir() / os.path.basename(src)`.
- **Fix direction (not applied):** In _local_image_bytes, resolve the candidate path and reject it unless it is contained in media_dir()/covers_dir() (e.g. path.resolve().is_relative_to(media_dir().resolve())), mirroring the basename guard already used for /covers/.

#### F12 · [SEC] Metadata provider SSRF: redirects followed without per-hop revalidation
- **Severity:** HIGH · **Area:** sweep-sec · **Confidence:** high · **Status:** new
- **Files:** `backend/app/integrations/metadata.py:90, backend/app/integrations/metadata.py:98, backend/app/integrations/metadata.py:106, backend/app/telemetry.py:82, backend/app/ingestion/netguard.py:100, backend/app/ingestion/fetcher.py:348`
- **How found:** Read MetadataProvider._request in integrations/metadata.py and compared to the hardened safe_get chokepoint in netguard.py and the imagecache/fetcher redirect handling.
- **Why flagged:** _request() validates the target with assert_public_url ONCE on the initial URL (metadata.py:98) but then issues the request through an httpx client created with follow_redirects=True (metadata.py:106). httpx follows 3xx hops itself, and telemetry.instrument() only records outcomes — it does NOT re-run the SSRF guard on each hop (telemetry.py:82-90). A public provider host (or an admin-set/operator base_url) that 302-redirects to http://169.254.169.254/… or an RFC-1918 address bypasses the guard entirely. The codebase already has the correct per-hop pattern (netguard.safe_get re-validates + IP-pins every hop with follow_redirects=False, and fetcher.py:348/572 does the same) — it was simply no…
- **Evidence:** metadata.py:106  async with telemetry.instrument("metadata", timeout=self._timeout, follow_redirects=True) as c: return await c.request(...)  # assert_public_url ran only on the initial url at line 98; redirect hops are not revalidated. Contrast netguard.safe_get: follow_redirects=False + assert_public_url(cur) every hop (IP-pinned).
- **Fix direction (not applied):** Route MetadataProvider._request through netguard.safe_get (or an async equivalent): follow_redirects=False and re-run assert_public_url + IP-pin on every hop.

#### F13 · [SEC] Non-admin SSRF via user-controlled Apprise notification channel URL
- **Severity:** HIGH · **Area:** sweep-sec · **Confidence:** high · **Status:** new
- **Files:** `app/routers/notifications.py:50, app/routers/notifications.py:91, app/notifications.py:158, app/notify.py:18, app/notify.py:27`
- **How found:** Traced UserSettings.apprise_url and NotificationChannel creation from the auth-gated (non-admin) notifications router into app/notify.py's Apprise call; confirmed apprise 1.11 is installed and no netguard is applied.
- **Why flagged:** Any logged-in user (no admin/permission gate) can POST /api/notifications/channels with kind="apprise" and an arbitrary config.url, or kind="ntfy" with an arbitrary server host. build_apprise_url passes the raw URL straight through (notifications.py:158-160) and notify() (notify.py:27-30) hands it to Apprise, which supports schemes like json://, jsons://, xml://, form://, ntfy://<host:port> that issue HTTP(S) requests to ANY host/port. Unlike every other outbound fetch in the app, this path NEVER calls netguard.assert_public_url, so it reaches 127.0.0.1, RFC1918 hosts, and cloud metadata (169.254.169.254). POST /api/notifications/channels/{id}/test (notifications.py:91-100) is a non-admin, o…
- **Evidence:** build_apprise_url: `if kind == "apprise": return (cfg.get("url") or "").strip() or None`; ntfy: `return f"ntfy://{auth}{host}/{topic}"` with host = arbitrary _host(cfg['server']). test_channel returns `{"ok": ok, "error": err}`. notify(): `ap.add(url); ap.notify(...)` with no assert_public_url anywhere in notify.py / deliver_to_channel.
- **Fix direction (not applied):** Validate the resolved Apprise target host with netguard.assert_public_url before add/deliver, and restrict allowed schemes to push services (block json/xml/form/file).

#### F14 · [UI] No visible focus indicator on primary interactive controls
- **Severity:** HIGH · **Area:** ui · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/frontend/src/components/ui.tsx:106, /root/Shelf/frontend/src/components/ui.tsx:240, /root/Shelf/frontend/src/App.tsx:99, /root/Shelf/frontend/src/index.css:1`
- **How found:** Grepped src for focus-visible/focus:ring/focus:outline; read ui.tsx Button/Tabs/Toggle and App.tsx Nav; confirmed built CSS via curl contains zero focus-visible rules.
- **Why flagged:** Button, Tabs, the top-nav NavLinks, Toggle, Badge-buttons, ThemeButton/UserButton and every icon button (✕, 🔔, 🎨) define no focus styling, and index.css has no global :focus-visible rule. Keyboard/switch users get no indication of focus — a WCAG 2.4.7 (Focus Visible) failure across the whole app. The few text inputs that do style focus use focus:outline-none with only a subtle focus:border-accent (1px color change), removing the browser default without a strong replacement.
- **Evidence:** ui.tsx Button base: "inline-flex items-center justify-center gap-2 rounded-lg font-medium transition disabled:opacity-50 …" (no focus:); built CSS: `curl .../index-CD19UXlV.css | grep -c focus-visible` => 0
- **Fix direction (not applied):** Add a global :focus-visible outline in index.css (or a shared focus:ring-2 ring-accent class on Button/Tabs/nav).

#### F15 · [BUG] delete_user orphans notifications and notification_channels rows
- **Severity:** MEDIUM · **Area:** backend-bugs · **Confidence:** high · **Status:** new
- **Files:** `backend/app/routers/auth.py:346-373, backend/app/models.py:839-883, backend/app/notifications.py:309-320`
- **How found:** Cross-referenced the bugs-review-5 D1 fix (commit 8b3f1ac) with the newer notifications feature commit (5b5a16c); confirmed delete_user was not updated to clean the new tables
- **Why flagged:** The notifications feature (commit 5b5a16c, newest) added `notifications` (user_id NOT NULL FK) and `notification_channels` (user_id nullable) tables. The D1 fix that cleaned up delete_user predates these tables. After deleting a user, their Notification rows remain in the DB permanently (data leak, wasted space) and their NotificationChannel rows remain enabled — an orphaned channel with push/email config could attempt delivery for future admin events routed to that user_id if any code re-queries it. FK enforcement is off in SQLite so no integrity error fires.
- **Evidence:** auth.py:346-373 deletes: BookshelfItem, Bookshelf, LibraryItem, Integration, UserSession, ReadingState, UserSettings — then db.delete(user). models.py:876 `user_id: Mapped[int] = mapped_column(ForeignKey('users.id'))` on Notification (NOT NULL). models.py:850 `user_id: Mapped[int | None]` on NotificationChannel. Neither table is in the delete_user cleanup.
- **Fix direction (not applied):** Add `db.execute(Notification.__table__.delete().where(Notification.user_id == user_id))` and `db.execute(NotificationChannel.__table__.delete().where(NotificationChannel.user_id == user_id))` before `db.delete(user)` in auth.py:delete_user

#### F16 · [OK] Comic-cover durable heal is genuinely progressing live
- **Severity:** MEDIUM · **Area:** karen · **Confidence:** high · **Status:** verify-prior
- **Files:** `/root/Shelf/backend/app/ingestion/catalog_enrichment.py:515, /root/Shelf/backend/app/ingestion/catalog_enrichment.py:545, /root/Shelf/backend/app/ingestion/scheduler.py:1085`
- **How found:** Read catalog_enrichment backfill + _COVER_BLOCKED; queried live DB cover stats; watched live AniList traffic and covers-dir mtimes
- **Why flagged:** Confirms the sticky AniList->durable /covers/ heal is wired into the scheduler tick and actively filling. Caveat: a long tail remains and drains gradually (bounded per tick).
- **Evidence:** Live DB: comic groups=88686, durable /covers/=60967, comix-CDN-blocked=0, legacy /media/imgcache=25127, null=2589. covers/ dir holds 111061 files; newest written 2026-06-15 18:10 (== current time). journalctl shows a steady stream of POST graphql.anilist.co 200s. _COVER_BLOCKED matches '/media/imgcache/%' (516) so legacy-evicted covers are re-sourced; sticky filter means healed rows aren't re-fetched. ~27.7k of 88.7k still in the actively-shrinking tail.
- **Fix direction (not applied):** Working; remaining backlog drains over time.

#### F17 · [NET] comix comic covers searched on AniList twice (enrich tick + backfill tick) for same row
- **Severity:** MEDIUM · **Area:** net · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/catalog_enrichment.py:349, /root/Shelf/backend/app/ingestion/catalog_enrichment.py:519, /root/Shelf/backend/app/ingestion/catalog_enrichment.py:545, /root/Shelf/backend/app/integrations/metadata_sync.py:243`
- **How found:** Traced catalog_enrichment._enrich_provider (catalog_enrich_tick) and backfill_comix_covers->fetch_cover_via_anilist (metadata_backfill_tick). Both call best_match(AniListProvider(), row.title, author, 'comic') for the same comic. The enrich tick already fetched the AniList Media (meta.cover_url available) and persisted enrich_ref['anilist']=id, but does NOT overwrite an existing comix.to cover_url…
- **Why flagged:** A comic row arriving with a Cloudflare-blocked static.comix.to cover gets matched on AniList during the enrich tick — which already has a usable AniList cover in meta.cover_url and stores the AniList id — yet the cover is discarded, and the backfill tick later runs a FRESH AniList search (not a cheaper by-id fetch via the stored enrich_ref) to get the same cover. One redundant AniList GraphQL search per blocked-cover comic row, also consuming the shared ~90rpm AniList throttle budget, with no UX benefit. Meaningful on a manhwa-heavy catalog.
- **Evidence:** _enrich_provider: `if meta.cover_url and not row.cover_url: row.cover_url = meta.cover_url` (won't replace a comix.to cover). fetch_cover_via_anilist: `bm = await best_match(AniListProvider(), row.title, ..., 'comic')` — fresh search, ignores extra['enrich_ref']['anilist'] persisted by _persist_identity.
- **Fix direction (not applied):** In _enrich_provider adopt meta.cover_url for comic rows whose existing cover is a comix.to/imgcache URL; OR have fetch_cover_via_anilist fetch by stored enrich_ref['anilist'] id instead of best_match.

#### F18 · [PERF-CPU] index_tick re-scans all 'done' sites for refresh on every ~10s tick
- **Severity:** MEDIUM · **Area:** perf · **Confidence:** medium · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/indexer.py:698, /root/Shelf/backend/app/ingestion/indexer.py:660, /root/Shelf/backend/app/ingestion/indexer.py:720`
- **How found:** Read index_tick + refresh_finished_sites; tick cadence default 10s (crawl_tuning) / 15s (config)
- **Why flagged:** Every index_tick (every ~10s) calls refresh_finished_sites(), which iterates all status='done' sites and runs a MAX(fetched_at) subquery per site, plus index_tick separately re-queries done sites for API/HTML refresh and runs an UPDATE...WHERE status='done' AND id IN (SELECT site_id FROM indexed_pages WHERE status='pending') revival check. These run unconditionally at tick cadence even when no site is due (refresh gated to 12h). Low absolute cost today (only 4 sites), but it is per-tick fixed overhead on the hot loop and the pending-subquery touches indexed_pages.
- **Evidence:** index_tick -> refresh_finished_sites(db, now) every tick; for site in done: MAX(fetched_at) per site; UPDATE IndexSite ... WHERE status='done' AND id IN (SELECT site_id FROM indexed_pages WHERE status='pending'). crawl_tuning tick_seconds default=10.
- **Fix direction (not applied):** Gate the done-site refresh/revival sweeps behind a coarser timer (e.g. every few minutes) rather than running them on every crawl tick.

#### F19 · [RISK] File-import endpoint has no upload size limit; archive bomb DoS
- **Severity:** MEDIUM · **Area:** security-audit · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/routers/jobs.py:169, /root/Shelf/backend/app/routers/jobs.py:181, /root/Shelf/backend/app/ingestion/media.py:349, /root/Shelf/backend/app/ingestion/media.py:441, /root/Shelf/backend/app/permissions.py:33`
- **How found:** Read import_file in routers/jobs.py and traced parse_media -> _comic_images/_parse_comic which fully decompress CBZ/CBR/EPUB into memory and write every page to disk; checked for any body-size cap app-wide.
- **Why flagged:** POST /api/works/import does `data = await file.read()` with no size bound, then parse_media() reads the whole archive into memory and writes every extracted page image to /media with no cap on entry count or decompressed size. A small malicious CBZ/zip (zip bomb) expands to GB of RAM/disk. The endpoint is gated only by the `add.use` permission, which is in DEFAULT_PERMISSIONS — every normal user has it. No reverse-proxy body limit exists (Cloudflare tunnel caps ~100MB but direct 0.0.0.0 LAN access has none, and a 100MB bomb still expands to many GB).
- **Evidence:** jobs.py l181 `data = await file.read()` (unbounded); media.py l353-355 `zf.read(name)` for every image entry into memory then l444 `(out_dir / fname).write_bytes(blob)`; no max-bytes / max-entries / decompressed-ratio guard anywhere; permissions.py DEFAULT_PERMISSIONS includes 'add.use'.
- **Fix direction (not applied):** Cap UploadFile size (stream-count bytes, reject >N MB), and in _comic_images/_parse_pdf bound entry count + cumulative decompressed bytes before writing.

#### F20 · [RISK] Live service binds 0.0.0.0 with cookie_secure off over plaintext HTTP
- **Severity:** MEDIUM · **Area:** security-scan · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/config.py:39, /root/Shelf/backend/app/config.py:82, /root/Shelf/backend/app/config.py:94, /root/Shelf/backend/app/auth.py:118`
- **How found:** Live recon: ss -ltnp + systemctl show Environment + curl header inspection on the running instance.
- **Why flagged:** shelf.service listens on 0.0.0.0:8000 (ss: LISTEN 0.0.0.0:8000 pid=329693) with no SHELF_COOKIE_SECURE, SHELF_ALLOWED_HOSTS, or SHELF_TRUST_PROXY set, so defaults apply (cookie_secure=False, allowed_hosts=['*']). The httpOnly session cookie is therefore issued without the Secure flag and travels over plaintext HTTP. If this port is reachable beyond loopback (it binds all interfaces), a network attacker on the LAN can sniff or replay the session cookie. HSTS is also never emitted because the app only sets it when proto==https. The config documents tunnel-mode hardening, so this is a deployment-posture gap, not a code defect.
- **Evidence:** ss: LISTEN 0 2048 0.0.0.0:8000 users:(("python",pid=329693)). systemctl Environment=SHELF_HOST=0.0.0.0 SHELF_PORT=8000 (no cookie/hosts/proxy vars). curl /api/health: no Strict-Transport-Security header. auth.py:118 secure = _is_secure(request) if request else settings.cookie_secure (default False).
- **Fix direction (not applied):** Behind a tunnel/reverse-proxy set SHELF_COOKIE_SECURE=1, SHELF_TRUST_PROXY=1, SHELF_ALLOWED_HOSTS=<domain>, and bind SHELF_HOST=127.0.0.1; or restrict :8000 at the firewall to loopback.

#### F21 · [BUG] backup stream_archive: writer thread error surfaced only after generator fully drains
- **Severity:** MEDIUM · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/backup.py:307-329`
- **How found:** Reading backup.py stream_archive lines 291-329
- **Why flagged:** The writer thread appends to `err` on failure (line 316). The generator yields chunks until EOF (the write end of the pipe closes on writer exit). After the loop the generator does writer.join(timeout=30) and THEN checks `err`. If the writer crashed early, the read side already saw EOF and the generator finished normally — the error is only raised after the generator completes, which is AFTER FastAPI has already sent the response headers and body (StreamingResponse already streamed partial bytes). The client receives a truncated zip with a 200 OK and no indication of the error.
- **Evidence:** err: list[BaseException] = [] ... yield chunk  # already sent to client ... writer.join(timeout=30) if err:     raise err[0]  # too late — response already sent
- **Fix direction (not applied):** Check err inside the yield loop (after each read) so streaming stops at the first detected writer error, giving the client a connection drop signal rather than a silently-truncated archive.

#### F22 · [BUG] libgen.grab does not deduplicate concurrent grabs for the same CatalogWork
- **Severity:** MEDIUM · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/libgen.py:932-961`
- **How found:** Reading libgen.py grab() lines 932-961 vs downloads.py grab_release which holds _grab_lock
- **Why flagged:** downloads.grab_release (usenet path) uses _grab_lock and a dedup query to prevent two simultaneous requests for the same book from creating duplicate DownloadJobs. libgen.grab (lines 932-961) has neither — it creates a DownloadJob directly without any lock or duplicate check. Two simultaneous acquire() calls resolving to the libgen route (e.g. two users acquiring the same catalog work) would both pass through, insert two separate DownloadJobs, and both race to download and import the same file, potentially creating two Works for the same book.
- **Evidence:** async def grab(db, cw, *, user_id=None, ...):     # no lock, no dedup check     ...     job = DownloadJob(catalog_work_id=cw.id, ...)     db.add(job)     db.commit()
- **Fix direction (not applied):** Add the same norm_key-cluster dedup query (checking for active libgen jobs) before inserting, protected by the asyncio _grab_lock from downloads.py, which is already imported in that module.

#### F23 · [BUG] _apply_series changes silently lost when add_to_library rolls back on import
- **Severity:** MEDIUM · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/backend/app/ingestion/downloads.py:728-748, /root/Shelf/backend/app/ingestion/libgen.py:914-926`
- **How found:** Tracing _import_completed (downloads.py:736-747) and _import_file (libgen.py:919-926): db.rollback() is called when add_to_library raises, but the recovery block only re-sets job.* and cw.hooked_work_id — not work.series / work.series_position set by _apply_series
- **Why flagged:** If add_to_library's db.commit() fails (e.g. IntegrityError on LibraryItem or BookshelfItem), all dirty ORM state is rolled back including the series name and series_position set by _apply_series. The recovery block (lines 742-747 in downloads.py, 925 in libgen.py) re-assigns job/cw fields but omits calling _apply_series again. The Work permanently loses its series grouping and ordering position even though it was imported successfully.
- **Evidence:** downloads.py line 735: `_apply_series(work, cw)` is called before `add_to_library`. If add_to_library raises at line 738 and db.rollback() is called at line 740, the recovery block at lines 742-747 sets job.work_id, job.verified, job.status, job.completed_at, cw.hooked_work_id — but NOT work.series or work.series_position. Identical omission in libgen.py at lines 922-925.
- **Fix direction (not applied):** Call `_apply_series(work, cw)` again inside the except block, after the other re-assignments, in both downloads._import_completed and libgen._import_file

#### F24 · [BUG] libgen _import_file: user never notified of successful import
- **Severity:** MEDIUM · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `backend/app/ingestion/libgen.py:914-928, backend/app/ingestion/downloads.py:749-754`
- **How found:** Code comparison: libgen.py _import_file vs downloads.py _import_completed post-import notification
- **Why flagged:** downloads.py _import_completed calls _notify_import(db, job, work) at line 754 for non-stock user grabs. libgen.py _import_file (lines 914-928) sets job.status='imported', calls add_to_library, and commits, but never dispatches any notification to the requesting user. A user who acquired a book via the libgen route (direct open-library grab) receives no 'Download completed' in-app notification or push alert. The parallel usenet/SABnzbd path always notifies.
- **Evidence:** libgen.py 914-928:   job.work_id, job.verified, job.status = work.id, True, 'imported'   ...   db.commit()   # NO dispatch_soon / _notify_import call  downloads.py 754:   else:       _notify_import(db, job, work)  # only in SAB path
- **Fix direction (not applied):** Add a dl._notify_import(db, job, work) call in libgen._import_file after the final db.commit(), mirroring the downloads.py pattern.

#### F25 · [BUG] libgen _import_file: cw.hooked_work_id set without None-guard when add_to_library rolls back
- **Severity:** MEDIUM · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `backend/app/ingestion/libgen.py:914-926`
- **How found:** Code trace: libgen.py _import_file rollback path
- **Why flagged:** At libgen.py line 916 cw.hooked_work_id = work.id is set before the add_to_library try/except. If add_to_library raises and db.rollback() fires at line 923, the cw mutation is also rolled back. Unlike downloads.py lines 746-747 which re-apply hooked_work_id after rollback, libgen.py only re-applies job fields (line 925) and does not re-set cw.hooked_work_id. After db.commit() the CatalogWork row will still have hooked_work_id=NULL even though the Work was promoted and imported — the Index will show the title as 'not hooked' and allow a second acquire that duplicate-imports the same book.
- **Evidence:** 914: job.work_id, job.verified, job.status = work.id, True, 'imported' 916: if cw.hooked_work_id is None: 917:     cw.hooked_work_id = work.id  # ← will be rolled back if add_to_library raises ... 922:     except Exception: 923:         db.rollback()   # ← discards cw.hooked_work_id=work.id 924:         log.exception(...) 925:         job.work_id, job.verified, job.status = work.id, True, 'imported'  # re-applied         # cw.hooked_work_id NOT re-applied
- **Fix direction (not applied):** After db.rollback() in the except block, re-apply cw.hooked_work_id = work.id (same pattern as downloads.py line 746-747) before db.commit().

#### F26 · [BUG] download job follower piggyback: follower.status not propagated to 'failed' for the primary-silent-fail path
- **Severity:** MEDIUM · **Area:** sweep-bugs · **Confidence:** high · **Status:** new
- **Files:** `backend/app/ingestion/downloads.py:1112-1116, backend/app/ingestion/downloads.py:670-678`
- **How found:** Code trace: downloads.py poll_tick verdict=='failed' (line 1114-1116)
- **Why flagged:** When _import_completed returns 'failed' (verified but file unplaceable / never became visible after _STALE_AFTER), line 1115 calls _fail_followers(db, followers, primary.error). At this point primary.status has already been set to 'failed' by _import_completed, but primary has NOT had _notify_failed called on it in poll_tick. _import_completed sets status='failed' and commits (e.g. line 678) but the primary user is never notified. Contrast with the SAB-reported-failure path at line 1117-1126 where _grab_next → cascade exhausted → _notify_failed is called via the exhausted path. For the 'verified but can't be placed' and 'never became visible' failures, the requesting user's 'Download failed'…
- **Evidence:** downloads.py 670-678 (_import_completed 'failed' path):   job.status = 'failed'   job.error = 'verified but could not promote into the library'   db.commit()   return 'failed' # No _notify_failed call here  poll_tick 1114:   else:  # failed       _fail_followers(db, followers, primary.error)       failed += 1 + len(followers) # No _notify_failed(db, primary) call for the primary
- **Fix direction (not applied):** After the 'else: _fail_followers(...)' on line 1115, add _notify_failed(db, primary) to alert the requesting user, mirroring the stale/SAB-failure paths.

#### F27 · [RISK] CBZ/CBR comic import has no decompressed-size cap (zip-bomb DoS)
- **Severity:** MEDIUM · **Area:** sweep-sec · **Confidence:** medium · **Status:** new
- **Files:** `backend/app/ingestion/media.py:349, backend/app/ingestion/media.py:353, backend/app/ingestion/media.py:441, backend/app/ingestion/media.py:444, backend/app/routers/jobs.py:169, backend/app/ingestion/local_folder.py:34`
- **How found:** Traced /works/import -> parse_media -> _parse_comic -> _comic_images; grepped media.py for any size/decompression guard (none found).
- **Why flagged:** _comic_images() reads every image-extension entry of an uploaded CBZ/CBR fully into memory with zf.read(name)/rf.read(name) (media.py:356/367) and _parse_comic then writes each blob to disk (media.py:444), with no per-entry or total DECOMPRESSED-size cap. A small highly-compressed archive (a handful of entries named *.png that inflate to GBs) can exhaust memory/disk. The raw HTTP upload is itself uncapped (prior finding), and the watched-folder path only caps the COMPRESSED file at 2 GiB (local_folder.py:34) — neither bounds the inflated output. Reachable by any user holding add.use (jobs.py:169) and by the local-folder scanner.
- **Evidence:** media.py:353  with zipfile.ZipFile(io.BytesIO(data)) as zf: names=[n for n in zf.namelist() if ext_of(n) in _IMAGE_EXTS]; for name in ...: entries.append((name, zf.read(name)))  # full decompress, no size check.  media.py:444  (out_dir / fname).write_bytes(blob)  # written with no running-total cap
- **Fix direction (not applied):** Before reading, sum ZipInfo.file_size / RarInfo.file_size and reject above a cap; also bound a running total while reading entries.

#### F28 · [SEC] Notification channels allow arbitrary-host (blind) SSRF via apprise/ntfy
- **Severity:** MEDIUM · **Area:** sweep-sec · **Confidence:** medium · **Status:** new
- **Files:** `backend/app/routers/notifications.py:91, backend/app/notifications.py:153, backend/app/notifications.py:159, backend/app/notifications.py:166, backend/app/notify.py:15, backend/app/routers/settings.py:128`
- **How found:** Read notifications router test/create endpoints and build_apprise_url; confirmed no netguard on the dispatch path.
- **Why flagged:** Any logged-in user (these routes are auth-gated, not admin) can create a channel and hit POST /notifications/channels/{id}/test. For kind='apprise' the raw user-supplied URL is passed straight to Apprise (notifications.py:159), and for kind='ntfy' the server host is user-controlled (notifications.py:166 -> _host()), yielding e.g. ntfy://user:pw@<internal-host>/topic. Apprise's json://, xml://, form:// (and ntfy http) plugins POST to arbitrary hosts including link-local/RFC-1918 with no assert_public_url check, giving a blind internal-network probe / request-forgery primitive. UserSettings.apprise_url (settings.py:128) is the same surface. Blind (no response body returned) and self-targeted, …
- **Evidence:** notifications.py:158  if kind == 'apprise': return (cfg.get('url') or '').strip() or None  # raw user URL -> Apprise, no host validation.  notify.py:27  ap = apprise.Apprise(); ap.add(url); ap.notify(...)  # fires POST to that host
- **Fix direction (not applied):** Validate the resolved channel host with assert_public_url (or restrict to known notification hosts) before ap.add()/notify; reject internal targets for apprise/ntfy server fields.

#### F29 · [SEC] Metadata provider HTTP client follows redirects without per-hop SSRF revalidation
- **Severity:** MEDIUM · **Area:** sweep-sec · **Confidence:** medium · **Status:** new
- **Files:** `app/integrations/metadata.py:90, app/integrations/metadata.py:98, app/integrations/metadata.py:106, app/routers/goodreads.py:69`
- **How found:** Read MetadataProvider._request in app/integrations/metadata.py: it validates only the initial URL with assert_public_url then issues the request with follow_redirects=True via telemetry.instrument.
- **Why flagged:** assert_public_url runs once on the initial URL (metadata.py:98), but the httpx client uses follow_redirects=True (metadata.py:106), so a 3xx can bounce the request onto an internal/metadata address that is never re-validated. Same class as the prior imagecache fix (which switched to follow_redirects=False + manual per-hop revalidation) but on a DISTINCT, unchanged code path. Provider base_urls are admin-only EXCEPT GoodreadsProvider, reachable by any logged-in user via PUT /api/me/goodreads — its goodreads.com fetch is issued through this same redirect-following client, so an upstream open redirect would be followed unvalidated on a non-admin-triggered request.
- **Evidence:** `await asyncio.to_thread(assert_public_url, url)` then `async with telemetry.instrument("metadata", timeout=self._timeout, follow_redirects=True) as c: return await c.request(method, url, ...)`. Contrast imagecache._get_client(): follow_redirects=False with per-hop assert_public_url in _fetch_image.
- **Fix direction (not applied):** Set follow_redirects=False on the metadata client and follow redirects manually, re-running assert_public_url on each hop (mirror imagecache._fetch_image).

#### F30 · [UI] bg-accent paired with hardcoded text-white fails contrast on light-accent themes
- **Severity:** MEDIUM · **Area:** ui · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/frontend/src/pages/Index.tsx:197, /root/Shelf/frontend/src/pages/Index.tsx:203, /root/Shelf/frontend/src/pages/Jobs.tsx:146`
- **How found:** Grepped 'bg-accent text-white'; cross-referenced accent colors in themes.ts.
- **Why flagged:** Index search mode toggle and Jobs fetch-filter pills hardcode `bg-accent text-white` instead of the theme token `text-accent-fg`. For themes whose accent is light (Gruvbox accent #fabd2f, E-ink accent #a7a79b — both with dark accentFg), white text on the light accent fails WCAG contrast and looks washed out. The rest of the app (Seg, Library tabs, Nav active link) correctly uses text-accent-fg, so this is an inconsistency that breaks on exactly those themes.
- **Evidence:** Index.tsx:197 `mode === "titles" ? "bg-accent text-white" : …`; themes.ts gruvbox: accent #fabd2f / accentFg #282828; eink: accent #a7a79b / accentFg #1c1c1c. Correct pattern at ReaderControls Seg: `value===key ? "bg-accent text-accent-fg"`.
- **Fix direction (not applied):** Replace `text-white` with `text-accent-fg` in those three spots.

#### F31 · [UX] Standalone titles with a series tag forced behind a series-modal click
- **Severity:** MEDIUM · **Area:** ux · **Confidence:** high · **Status:** new
- **Files:** `/root/Shelf/frontend/src/pages/Library.tsx:819-875`
- **How found:** Read Library.tsx buildGridItems + SeriesLibraryCard rendering logic
- **Why flagged:** Any work that carries a `series` value is collapsed into a 'Series' card even when only ONE volume is in the library. The card shows a 'Series' badge + '1 in library' and has no direct Read button on the grid — the user must open the series modal, wait for a workSeries() fetch, then click Read. For a single owned book this is pure friction versus a normal card that reads on one tap. Many catalog/imported books legitimately have a series field set even when the user owns just one.
- **Evidence:** buildGridItems: `if (w.series) { ... out.push({ kind: 'series', name: w.series, books }) }` with no `books.length > 1` guard. SeriesLibraryCard always renders 'Open series' (modal) and Badge `{books.length} in library`, never a direct Read link.
- **Fix direction (not applied):** Only collapse into a series card when 2+ owned volumes share the series; render a single owned volume as a normal work card.


---

### Appendix A — 61 unverified-low findings (single-pass, not multi-vote verified)

These passed a finder but were not adversarially verified (severity=low). Triage before acting;
some may be noise. Listed for completeness.

- [BUG] catalog regroup leaves brief partial-visibility window for cache misses — `backend-bugs` — backend/app/ingestion/catalog_groups.py:239-299, backend/app/ingestion/catalog_groups.py:306-310
- [PERF-CPU] Async-route blocking: heavy paths offloaded; point queries still on loop by design — `karen` — /root/Shelf/backend/app/routers/index.py:669, /root/Shelf/backend/app/routers/reading.py:173
- [NET] Per-row metadata API calls not coalesced across same-title rows within one tick — `net` — /root/Shelf/backend/app/ingestion/matchmeta.py:260, /root/Shelf/backend/app/ingestion/catalog_enrichment.py:276
- [OK] Image/cover caching, idle-poll skip, AniList batching verified well-optimized — `net` — /root/Shelf/backend/app/imagecache.py:228, /root/Shelf/backend/app/routers/imgproxy.py:57
- [PERF-IO] imagecache _existing_local does up to 7 stat() syscalls per image hit — `perf` — /root/Shelf/backend/app/imagecache.py:119, /root/Shelf/backend/app/imagecache.py:307
- [RISK] Stale multi-GB DB backup copies inflate disk usage — `perf` — /root/Shelf/backend/shelf.db.pre-libclean-211514, /root/Shelf/backend/shelf.db.pre-relink-20260611-174030
- [RISK] Send-to-Kindle allows emailing to arbitrary recipient via shared SMTP — `security-audit` — /root/Shelf/backend/app/routers/delivery.py:326, /root/Shelf/backend/app/routers/delivery.py:340
- [SEC] Verify prior fix: netguard per-hop SSRF re-validation and IP pinning — `security-audit` — /root/Shelf/backend/app/ingestion/netguard.py:100, /root/Shelf/backend/app/ingestion/netguard.py:117
- [SEC] Verify prior fix: backup restore zip-slip path containment — `security-audit` — /root/Shelf/backend/app/backup.py:681, /root/Shelf/backend/app/backup.py:703
- [SEC] Verify prior fix: ComicInfo.xml parsed with defusedxml (XXE) — `security-audit` — /root/Shelf/backend/app/ingestion/media.py:306, /root/Shelf/backend/app/ingestion/media.py:282
- [SEC] Verify prior fix: SMTP refuses AUTH over unencrypted connection — `security-audit` — /root/Shelf/backend/app/kindle.py:149, /root/Shelf/backend/app/kindle.py:80
- [RISK] Dev CORS origin (localhost:5173) left enabled with credentials in prod — `security-scan` — /root/Shelf/backend/app/config.py:155, /root/Shelf/backend/app/main.py:107
- [OK] Verify prior fix: /media and /covers static mounts session-gated + traversal-safe — `security-scan` — /root/Shelf/backend/app/static_auth.py:66, /root/Shelf/backend/app/main.py:186
- [OK] Verify prior fix: imgproxy SSRF guard + allowlist on /api/cover and /api/img — `security-scan` — /root/Shelf/backend/app/routers/imgproxy.py:57, /root/Shelf/backend/app/routers/imgproxy.py:91
- [OK] Verify prior fix: login brute-force throttle returns 429 after threshold — `security-scan` — /root/Shelf/backend/app/config.py:96, /root/Shelf/backend/app/routers/auth.py:106
- [OK] Dependency CVE audit clean: backend pip + frontend npm — `security-scan` — /root/Shelf/backend/pyproject.toml, /root/Shelf/frontend/package.json
- [RISK] scheduled_backup_tick calls backups_store.start_build then blocks for notification I/O on same thread — `sweep-bugs` — /root/Shelf/backend/app/ingestion/scheduler.py:1143-1153
- [BUG] follower job completed_at is NULL after add_to_library rollback in _propagate_import — `sweep-bugs` — /root/Shelf/backend/app/ingestion/downloads.py:798-815
- [BUG] cleanup_download_jobs_tick runs blocking filesystem I/O on the event loop — `sweep-bugs` — /root/Shelf/backend/app/ingestion/scheduler.py:1096-1101, /root/Shelf/backend/app/ingestion/downloads.py:184-227
- [BUG] dispatch_soon in _import_completed thread calls dispatch_event blocking SMTP on worker thread with caller's DB session — `sweep-bugs` — /root/Shelf/backend/app/ingestion/downloads.py:754, /root/Shelf/backend/app/ingestion/downloads.py:962-971
- [BUG] storage_path assignment at poll_tick is not committed before asyncio.to_thread call — `sweep-bugs` — /root/Shelf/backend/app/ingestion/downloads.py:1090-1095
- [BUG] bulk_download_get IDs parser incorrectly allows negative integers through filter — `sweep-bugs` — /root/Shelf/backend/app/routers/delivery.py:315
- [PERF-IO] _enqueue_links loads entire site URL set per page fetch, O(N) per crawled page — `sweep-bugs` — /root/Shelf/backend/app/ingestion/indexer.py:547-551
- [BUG] _notify_failed silently skips primary-failed notification when user_id is None (stock jobs fail silently) — `sweep-bugs` — backend/app/ingestion/downloads.py:832-844
- [BUG] sweep_orphan_staging uses naive datetime for grace-window cutoff, inconsistent with UTC comparisons — `sweep-bugs` — backend/app/ingestion/downloads.py:202
- [RISK] Shared-crawl mutation (check-updates/repair/pause/resume) open to any member — `sweep-sec` — backend/app/routers/works.py:276-290, backend/app/routers/works.py:346-357
- [NET] Per-user cover proxy allows SSRF to any public host (no allowlist) — `sweep-sec` — backend/app/routers/imgproxy.py:57-88, backend/app/imagecache.py:186-225
- [SEC] /media and /covers gate authenticates but does not authorize per-user — `sweep-sec` — backend/app/static_auth.py:29, backend/app/static_auth.py:66
- [OK] Verify prior fix: imagecache/safe_get redirects re-validate + IP-pin — `sweep-sec` — backend/app/imagecache.py:53, backend/app/imagecache.py:197
- [SEC] Unauthenticated /health leaks disk-free and WAL-size telemetry — `sweep-sec` — app/main.py:138, app/routers/health.py:58
- [SEC] /api/cover open-redirect allowlist includes the entire /api/ prefix — `sweep-sec` — app/routers/imgproxy.py:60
- [OK] Verify prior fix: ComicInfo.xml + EPUB parsed without XXE exposure — `sweep-sec` — app/ingestion/media.py:306, .venv/lib/python3.12/site-packages/ebooklib/utils.py:34
- [OK] Verify prior fix: comic/EPUB archive extraction is not zip-slip-able — `sweep-sec` — app/ingestion/media.py:436, app/ingestion/media.py:60
- [UI] Native checkboxes ignore theme accent (render browser-default blue) — `ui` — /root/Shelf/frontend/src/pages/Library.tsx:56, /root/Shelf/frontend/src/pages/Users.tsx:30
- [UX] Several controls below ~44px mobile hit target — `ui` — /root/Shelf/frontend/src/components/ReaderControls.tsx:44, /root/Shelf/frontend/src/components/ui.tsx:292
- [UI] Toggle switch knob hardcoded bg-white — `ui` — /root/Shelf/frontend/src/components/ui.tsx:141
- [UX] Two parallel 'add a work' entry points (Add vs Index) confuse discovery — `ux` — /root/Shelf/frontend/src/App.tsx:123-124, /root/Shelf/frontend/src/pages/Library.tsx:596-598
- [UI] Send dialog hardcodes 'EPUB' wording even for comics (CBZ) — `ux` — /root/Shelf/frontend/src/components/SendDialog.tsx:67-113, /root/Shelf/frontend/src/pages/Library.tsx:801-802
- [UX] Reader keyboard shortcuts are undiscoverable (no help/legend) — `ux` — /root/Shelf/frontend/src/pages/Reader.tsx:259-277, /root/Shelf/frontend/src/components/ReaderControls.tsx:152-318
- [UI] Stock 'Cap' input max=2000 but mutation clamps to 5000 (inconsistent ceiling) — `ux` — /root/Shelf/frontend/src/pages/Stock.tsx:104-106, /root/Shelf/frontend/src/pages/Stock.tsx:161-164
- [UX] Library 'Check updates' (check all) is admin-only with no hint for non-admins — `ux` — /root/Shelf/frontend/src/pages/Library.tsx:586-595, /root/Shelf/frontend/src/pages/Library.tsx:758-772
- [UX] Reader FAB draggability and reveal-tab are undiscoverable on first use — `ux` — /root/Shelf/frontend/src/components/ReaderFab.tsx:116-129, /root/Shelf/frontend/src/components/ReaderFab.tsx:159-167
- [OK] Empty notification dropdown and TOC error states are adequate (spot-check) — `ux` — /root/Shelf/frontend/src/pages/Library.tsx:615-640, /root/Shelf/frontend/src/pages/Reader.tsx:388-402
- [DEADCODE] library_status() 'fetched' param dead — 5 callers pass it, body never reads it — `wiring-deadcode` — backend/app/routers/works.py:95, backend/app/routers/works.py:183
- [DEADCODE] Eight unused imports across backend (ruff F401) — `wiring-deadcode` — backend/app/backup.py:50, backend/app/ingestion/flaresolverr.py:28
- [DEADCODE] Unused local variables in ingestion (ruff F841) — `wiring-deadcode` — backend/app/backup.py:575, backend/app/ingestion/comix_catalog.py:224
- [DEADCODE] liber3 book provider is a hardcoded empty stub but selectable via config — `wiring-deadcode` — backend/app/ingestion/libgen.py:711, backend/app/ingestion/libgen.py:76
- [DEADCODE] Four one-time sentinel migrations re-run guard logic every boot (already ran) — `wiring-deadcode` — backend/app/main.py:64, backend/app/ingestion/indexer.py:594
- [DEADCODE] Dead route: GET /img (image proxy) never referenced by frontend — `wiring-deadcode` — backend/app/routers/imgproxy.py:91, backend/app/imagecache.py:309
- [DEADCODE] Dead end-to-end route: GET /works/{id}/diagnose (UI only calls /repair) — `wiring-deadcode` — backend/app/routers/works.py:331, frontend/src/api/client.ts:1265
- [DEADCODE] Dead end-to-end route: POST /works/{id}/unhook never called by UI — `wiring-deadcode` — backend/app/routers/jobs.py:150, frontend/src/api/client.ts:1002
- [DEADCODE] Dead end-to-end route: GET /catalog/{id}/routes never called by UI or backend — `wiring-deadcode` — backend/app/routers/index.py:1292, frontend/src/api/client.ts
- [DEADCODE] Dead end-to-end route: GET /catalog/{id}/releases never called by UI — `wiring-deadcode` — backend/app/routers/index.py:1126, frontend/src/api/client.ts:1348
- [WIRING] Half-wired feature: POST /index/blocks (create) never called — UI only lists/deletes blocks — `wiring-deadcode` — backend/app/routers/index.py:1446, frontend/src/api/client.ts:1252
- [DEADCODE] Dead end-to-end route: POST /stock/sweep (integrity re-check) never called — `wiring-deadcode` — backend/app/routers/stock.py:67
- [DEADCODE] Dead end-to-end routes: GET /stock and POST /stock/clear (listStock/clearStock unused) — `wiring-deadcode` — backend/app/routers/stock.py:50, backend/app/routers/stock.py:137
- [DEADCODE] Dead route: GET /kindle/status never called (UI reads smtp_configured from /settings) — `wiring-deadcode` — backend/app/routers/delivery.py:319
- [DEADCODE] Dead route: POST /me/goodreads/test never called by UI — `wiring-deadcode` — backend/app/routers/goodreads.py:118
- [DEADCODE] Likely dead route: GET /admin/backups/{name}/download (UI downloads via /admin/backup) — `wiring-deadcode` — backend/app/routers/backup.py:104, frontend/src/api/client.ts:1086
- [RISK] Stock query ['catalog-categories', media] not invalidated after queue/clear — `wiring-deadcode` — frontend/src/pages/Stock.tsx:94, frontend/src/pages/Stock.tsx:116
- [OK] FE↔BE route + schema reconciliation: no broken calls or type drift found — `wiring-deadcode` — frontend/src/api/client.ts, backend/app/schemas.py


### Appendix B — 28 rejected findings (false positives killed by verification)

Recorded for transparency — these were claimed by a finder but refuted on multi-vote verification.

- verify-prior: backup zip-slip containment (_zip_entry_dest) — `backend-bugs` (critical)
- verify-prior: CrawlJob two-writer lease race (scheduler.py) — `backend-bugs` (high)
- verify-prior: sweep_orphan_staging shared-drop-zone blast radius — `backend-bugs` (high)
- verify-prior: fetcher cookie-ordering x2 (zendriver + FlareSolverr) — `backend-bugs` (high)
- verify-prior: DNS-rebind in netguard assert_public_url — `backend-bugs` (high)
- verify-prior: poll_tick libgen exclusion — `backend-bugs` (high)
- verify-prior: blocking-IO-on-loop in download import — `backend-bugs` (high)
- verify-prior: delete_user orphans (D1 bugs-review-5) — `backend-bugs` (high)
- verify-prior: request_stats outcome key dimension — `backend-bugs` (medium)
- verify-prior: regroup per-bucket yield and watermark — `backend-bugs` (medium)
- FTS5 stores a full duplicate copy of indexed_pages text (~371 MB) — `perf` (medium)
- Orphan-staging sweep skips non-standard SAB layouts (guard, not assert) — `karen` (medium)
- Comix Turnstile crawl: browser works live, but parent json.loads of full stdout discards the result — `karen` (high)
- Alembic-vs-create_all duality: create_all is sole builder; migrations do NOT build same schema (by design) — `karen` (medium)
- dispatch_soon called from asyncio.to_thread worker shares caller DB session — `sweep-bugs` (high)
- sweep_orphan_staging uses naive datetime.now() vs UTC-aware cutoff — `sweep-bugs` (medium)
- auto_kindle_tick calls dispatch_event (blocking I/O) on the scheduler threadpool session — `sweep-bugs` (medium)
- _propagate_import rolls back after add_to_library failure but leaves f.status=imported dirty in-memory — `sweep-bugs` (medium)
- backup ad-hoc stream endpoint has no auth guard — `sweep-bugs` (high)
- stock.py clear_stock bulk-deletes stocked items without deleting their files — `sweep-bugs` (medium)
- reap_stalled_jobs: dedup re-check before inserting is not atomic under concurrent reapers — `sweep-bugs` (medium)
- auto-Kindle tick sends SMTP email on the event loop (not offloaded to thread) — `sweep-bugs` (medium)
- _import_completed: folder-sync before Work lookup can import file twice in concurrent scenario — `sweep-bugs` (medium)
- grab_release follower creation copies status=retry when primary is in retry state — `sweep-bugs` (medium)
- primary.storage_path not committed before asyncio.to_thread(_import_completed) runs — `sweep-bugs` (medium)
- reap_stalled_jobs race: two jobs created per work when hooked_work_id set but no open job — `sweep-bugs` (medium)
- grab_release: follower job created with nzo_id=None when primary is in 'deferred' status — `sweep-bugs` (medium)
- dispatch_soon from asyncio.to_thread worker creates dangling executor futures without join — `sweep-bugs` (medium)


---

## STAGE 3 — COORDINATED FIX PLAN  *(no code changes until approved)*

**Principles:** (1) batch by subsystem so two fixes never edit the same region in separate passes;
(2) build shared infrastructure before its consumers; (3) safe quick-wins before risky structural
changes; (4) every batch ends with a concrete verify step; (5) schema changes go in BOTH the
alembic migration AND the `create_all`/`_ADDITIVE_COLUMNS` boot path — note the rejected-finding
caveat that `create_all` is currently the sole schema builder, so any new column/index must be
added there or it won't exist at runtime.

### Fix-vs-fix conflict map (read before sequencing)
| Coupling | Items | Rule |
|---|---|---|
| Same `downloads.py` recovery block | F09, F10, F26 | One atomic edit — separate passes clobber each other |
| Same `libgen.py` import block | F23, F24, F25 (+ F22 nearby) | One atomic edit; do before F22 |
| Both import paths, identical bug class | {F09,F10} ↔ {F23,F25} | Extract a shared finalize/recovery helper so the class can't re-diverge |
| Regroup path | F05 → F01 | F05 (safe, localized) before F01 (structural); both feed `regroup` |
| Same `indexer.py` | F06, F18 | Coordinate edits |
| Shared netguard egress primitive | F08, F12/F29, F13/F28 | Build ONE per-hop IP-pinning helper, apply to all — divergence caused these gaps |
| Must ship together | F19 + F27 | Either alone still allows the DoS |
| Alembic + boot schema | F07 (partial index), F04 (etag cols) | Sequential revisions; mirror into the boot builder |

### Batches (in execution order)

**Batch 0 — Deployment / housekeeping (no app-code change, lowest risk).**
- **F20** posture: behind the tunnel set `SHELF_COOKIE_SECURE=1`, `SHELF_TRUST_PROXY=1`,
  `SHELF_ALLOWED_HOSTS=<domain>`, bind `SHELF_HOST=127.0.0.1` (or firewall `:8000` to loopback).
- Housekeeping (operator confirm): the three stale ~3 GB DB backups in `backend/` (~9 GB) [Appendix A].
- _Verify:_ `ss -ltnp` shows loopback bind; cookie carries `Secure`; disk reclaimed.

**Batch 1 — SSRF hardening (security; shared infra first).** Build a single per-hop IP-pinning
egress helper (extend `netguard.safe_get` / add async variant), then route through it:
- **F08** imagecache `_fetch_image` → pin to validated IP each hop.
- **F12 + F29** metadata provider `_request` → `follow_redirects=False` + re-validate each hop.
- **F13 + F28** apprise/ntfy notify path → `assert_public_url` on resolved host + scheme allowlist.
- _Verify:_ mock DNS-rebind + redirect-to-internal blocked; legit AniList/OpenLibrary/Goodreads/
  ntfy still work.  _Risk: medium — too-tight allowlist could break a real provider._

**Batch 2 — File/archive safety (security/DoS).**
- **F11** `epub_export._local_image_bytes` → containment check (mirror the `/covers/` basename guard).
- **F19 + F27** cap upload body bytes (`jobs.py`) AND decompressed size/entry-count (`media.py`,
  `local_folder.py`). _Verify:_ `/media/../etc/hostname` rejected; zip-bomb rejected; normal CBZ/EPUB ok.

**Batch 3 — Import-path transaction recovery (correctness; tightly coupled).** One coordinated edit
per file, ideally via a shared `_finalize_import()` helper:
- **F09 + F10 + F26** `downloads.py:_import_completed` — re-apply `hooked_work_id` + `_apply_series`
  in the rollback-recovery block; notify primary on silent-fail.
- **F23 + F24 + F25** `libgen.py:_import_file` — same recovery fixes + add success notification.
- _Verify:_ force `add_to_library` failure → series + hook persist; libgen success → user notified.

**Batch 4 — Regroup CPU/disk (the centerpiece; addresses the CRITICAL).**
- **F05** precompute author token-sets once in `_union_find_groups` (catalog.py) — ~34s→0.4s/run,
  localized, safe. **Do first.**
- **F01** make `regroup` incremental (only re-cluster touched buckets) or gate on a min row-delta —
  structural, higher risk. _Verify (critical):_ group assignments byte-identical to a full rebuild
  on a DB snapshot; then measure write/CPU drop.  _Risk: high — changes clustering semantics._

**Batch 5 — Hot-loop query reductions (perf, independent, low-risk).**
- **F06** `_enqueue_links` → bounded `url IN (:candidates)` existence query (indexer.py).
- **F18** gate done-site refresh/revival behind a coarser timer (indexer.py).
- **F07** partial index on `cover_url WHERE LIKE 'http%'` (+ migration + boot schema) and back off the
  30s cover-cache cadence once the remote backlog is empty (scheduler.py).
- _Verify:_ `EXPLAIN` shows index use; per-tick CPU drops; crawl still discovers pages.

**Batch 6 — External-request minimization (net).**
- **F04** wire conditional GET: add `etag`/`last_modified` columns (migration + boot schema), persist
  on fetch, pass back into `fetcher.get()`, treat 304 as unchanged-skip-parse. Highest request saver;
  touches many adapters + a migration. _Verify:_ unchanged-TOC refresh returns 304 / skips parse.
- **F17** comix cover: adopt the enrich-tick AniList cover (or fetch-by-stored-id) instead of a 2nd
  search (catalog_enrichment.py). _Verify:_ ≤1 AniList request per blocked-cover row.

**Batch 7 — Correctness misc (independent).**
- **F15** `delete_user` → delete `notifications` + `notification_channels` rows (auth.py).
- **F21** `backup.stream_archive` → check writer error inside the yield loop (backup.py).
- **F22** `libgen.grab` → dedup lock + active-job check (libgen.py; sequence after Batch 3's libgen edit).
- _Verify:_ deleted user leaves no orphan rows; backup writer error drops the connection; concurrent
  libgen grabs dedup to one job.

**Batch 8 — UI/UX (frontend, isolated from backend).**
- **F14** global `:focus-visible` outline (index.css / ui.tsx) — WCAG 2.4.7.
- **F30** `text-white` → `text-accent-fg` in Index.tsx (×2) + Jobs.tsx.
- **F31** only collapse into a series card when 2+ owned volumes (Library.tsx).
- _Verify:_ keyboard focus visible; contrast passes on Gruvbox/E-ink; single owned volume reads in one tap.

### Suggested PR grouping & risk
- **Ship-first (low risk, high value):** Batch 0, Batch 1, Batch 2 (security), Batch 8 (UX), F05, F15.
- **Needs careful testing:** F01 (clustering semantics), F04 (migration + many call sites), Batch 3
  (transaction semantics).
- **Migrations introduced:** F07 (partial index), F04 (etag columns) — sequential alembic revisions,
  mirrored into the boot builder.
- **Appendix A (61 lows) to triage first** — several are worth promoting (e.g. the dead-route cleanup
  cluster, the "dev CORS origin enabled with credentials" RISK, the duplicate FTS-copy storage, the
  blocking-I/O-on-event-loop bugs). Not yet adversarially verified, so confirm before acting.

> **Next step:** await approval of this plan. On approval, implement batch-by-batch (each batch its
> own change + verify), re-running the relevant backend tests after each. Recommend starting Batch 0/1/2.

