================================================================================
SHELF — FIX IMPLEMENTATION REPORT
Companion to: fix_issues.txt (findings) + fix_implementation.txt (the plan)
Generated by: Claude (Opus 4.8) — full execution of the fix plan
Last updated: 2026-06-12
================================================================================

This document records (Part A) every fix implemented against the plan, with the
backing commit(s), and (Part B) the results of a fresh end-of-program sweep of the
codebase and the running app (Playwright). Each plan item is marked:
  [DONE]    implemented + tested + live
  [DONE*]   implemented via a documented ALTERNATIVE (the plan's literal approach
            was unsafe/infeasible; an equivalent solution was shipped instead)
  [PARTIAL] the safe/high-value part shipped; a larger follow-up is noted

Workflow for every item: edit -> pytest (targeted) -> full suite -> commit ->
systemctl restart shelf -> verify /api/health + migrations. Backend test suite at
end of program: 643 passed, 4 skipped. Frontend: tsc + vite build clean.


================================================================================
PART A — IMPLEMENTATION BY PLAN SECTION
================================================================================

--------------------------------------------------------------------------------
SECTION 0 — FOUNDATIONS (F0.x)
--------------------------------------------------------------------------------
[DONE] F0.1  SSRF safe_get() chokepoint in netguard; all ad-hoc export/cover fetches
       routed through it (per-hop re-validation).                  commit 5fb166e
[DONE] F0.2  Race-hardening unique indexes + insert_or_reuse (IntegrityError ->
       adopt the winner row).                                       commit 6834d2e
[DONE] F0.3  CrawlJob run-lease tokens — single writer for the job lifecycle. 6834d2e
[DONE] F0.4  Shared full-body Cloudflare challenge detector + RenderedPage headers/
       original-status passthrough.                                 commit e1bc079
[DONE] F0.5  Focus-trapping Modal primitive (useDialogFocus) across all dialogs. 5509a84
[DONE] F0.6  @scheduled_task decorator: owns SessionLocal/try/rollback/close +
       optional to_thread; 10 uniform ticks converted. SHELF_LOG_LEVEL honoured;
       uvicorn access_log off in prod; real /health readiness (SELECT 1 + free disk
       + WAL size, 503 on degraded).                       commits a23da37, 3de4269
[DONE] F0.7  Atomic promote + per-destination lock.                 commit 6834d2e

--------------------------------------------------------------------------------
SECTION 1 — SECURITY (S1..S6)
--------------------------------------------------------------------------------
[DONE] S1  Backup zip-slip RCE fix (path containment on restore).   commit e7d0f58
[DONE] S2  Auth-gate the /media and /covers static mounts.          commit 1476424
[DONE] S3/S4/S5  SMTP STARTTLS/SSL, session revocation, brute-force store. 5fb166e
[DONE] S6  Lower-risk hardening batch: setup fail-closed behind an untrusted proxy,
       sanitize.py img-src scheme filter, cookie_secure/Origin.     commit 3860cbf
[DONE] S6-DNS  DNS-rebinding: assert_public_url returns the validated IPs and
       safe_get now CONNECTS to that exact IP (httpx URL host = IP) while preserving
       the real Host header + TLS SNI/cert hostname (sni_hostname extension), closing
       the resolve-then-connect TOCTOU window. Verified against a live HTTPS host
       (cert-verified against the hostname while connected to the IP).  commit f956f08

--------------------------------------------------------------------------------
SECTION 4 — DATA / SCHEMA INTEGRITY (F4.x)
--------------------------------------------------------------------------------
[DONE] F4.1/F4.2  reading_states NULL-key crash fix + restore PK remap cleanup. 3860cbf
[DONE*] F4.3  Alembic is now the schema VERSION authority + forward-migration applier
       on boot. The chain is incremental (early revisions assume a create_all
       baseline — 0024 references stock_items it never creates), so create_all still
       BUILDS the schema; _sync_schema_version() then STAMPS an already-built/unstamped
       DB at head (never replaying create_all-dependent revisions) and UPGRADEs a
       stamped DB that's behind head, so a new revision auto-applies on boot. Live DB
       stamped at 0025. Full "alembic-only" (drop create_all + a generated baseline
       revision) is the larger noted follow-up.                       commit 184b19c
[DONE] F4.4  Collision-safe storage keys (covers.py/media.py hash a key's tail past
       the 120-char filename limit; short keys stay backward-compatible); tz-aware
       datetime.now(UTC) in backup/backups_store; _ADDITIVE_COLUMNS tables asserted to
       be real mapped tables.                                         commit b1147d8

--------------------------------------------------------------------------------
SECTION 2 — CONCURRENCY (C1..C5)
--------------------------------------------------------------------------------
[DONE] C1-C5  Idempotent stock-mark, no duplicate primary download, fixed semaphore,
       cache invalidation on mutation.                               commit 1ee91c1
[DONE] C4b  cache "catalog" cleared on remove_from_library / orphan prune (works.py
       delete path) — already present, verified.

--------------------------------------------------------------------------------
SECTION 3 — INGESTION CORRECTNESS (I1..I8)
--------------------------------------------------------------------------------
[DONE] I1-I4,I8  Revive series auto-advance, decouple libgen from stock_tick,
       per-chapter attempt accounting, un-strand stock.             commit 8cc3996
[DONE] I5  poll_tick re-homes live followers to a live primary instead of failing
       a SAB-untracked group.                                        commit 838c967
[DONE] I6  Refresh-job branch finishes after discover_updates (hands pending chapters
       to a backfill); periodic refresh not suppressed by a long backfill. 838c967
[DONE] I7  Speculative re-probe (skipped->pending) no longer inflates
       total_chapters_known; distinguished from real growth by row-count delta. 838c967

--------------------------------------------------------------------------------
SECTION (CC) — CONTENT CORRECTNESS
--------------------------------------------------------------------------------
[DONE] CC1-CC4  Descramble retries incomplete renders, rank-based progress %, spine
       order, embed remote <img> via safe_get.                      commit 1377ec8
[DONE] CC5  EPUB/comic correctness: _norm_lang (whole BCP-47 subtags, no mid-subtag
       truncation), _comic_page_order (preserve archive order when names aren't
       reliably numeric), PDF outline page-dedup, escaped _xhtml_body fallback.
       (Webtoon layout already handled client-side by ComicReader's aspect probe.)
                                                                     commit 2d37e97

--------------------------------------------------------------------------------
SECTION 5 — PERFORMANCE (P1..P7)
--------------------------------------------------------------------------------
[DONE] P1,P4-P6  Catalog popularity index, smaller per-connection cache, /jobs
       pagination, image disk-cache for the proxy.                  commit c6d261b
[DONE] P2  Search no longer drops low-popularity matches at the 2000 popularity cap —
       a filtered query widens the candidate ceiling to 20000 (browse keeps the 2000
       popularity slice; book_catalog's own limit=40 probe untouched).  commit 9646096
[DONE*] P3  regroup memory: the global union-find needs all rows resident, but it only
       ever merges rows of the SAME media bucket — so regroup now loads + clusters ONE
       bucket at a time (comic, then non-comic incl. NULL media_kind) with yield_per,
       freeing each before the next. Peak memory = larger bucket, not the whole table.
       Zero correctness change (full ORM rows, same helpers).        commit df0bb8f
[DONE] P7  Comic->Kindle export tracks raw page bytes and fails the email cap early
       (4x-cap memory ceiling) instead of materializing every page + building the
       whole EPUB before the post-build 413.                        commit 495ca8c

--------------------------------------------------------------------------------
SECTION (M) — MOBILE / PWA
--------------------------------------------------------------------------------
[DONE] M1  PWA: manifest.webmanifest (standalone, theme/bg, 192/512 + maskable icons),
       themed book-glyph icons + apple-touch + favicon, linked in index.html. Hand-
       written dependency-free service worker with explicit security rules: app shell
       network-first (no stale-after-deploy), cache-first ONLY for hashed /assets/* and
       content-addressed images (/media/*, /api/cover), NEVER caches /api/* (chapters/
       per-user JSON), /auth/*, non-GET or 401/3xx. skipWaiting + clients.claim;
       registered PROD-only.                                         commit 911becc
[DONE] M2  Touch: Library continue-reading controls visible on touch; overscroll-
       contain on reader + comic scroll containers; ReaderFab setPointerCapture;
       #root min-height:100dvh.                                      commit 911becc

--------------------------------------------------------------------------------
SECTION (O) — OPS
--------------------------------------------------------------------------------
[DONE] O1-O3  Graceful shutdown, scheduled auto-backups + retention, image-cache LRU
       eviction.                                            commits a23da37, 46adc53
[DONE] O4  systemd: generated unit (and example) now Type=notify + NotifyAccess=main +
       WatchdogSec=120 + KillMode=mixed; app sends sd_notify READY=1 once serving and
       pings the watchdog (app/sdnotify.py, no-op off-systemd). install.sh free-port
       probe dies loudly instead of emitting an already-busy port.   commit 4f49a0b

--------------------------------------------------------------------------------
SECTION 10 — DOCS (D1..D3)
--------------------------------------------------------------------------------
[DONE] D1-D3  Auto-update toggle clarified, SHELF_SETUP_TOKEN documented as required
       on a public address, uuid-migration-plan marked unimplemented, service-file
       framed as an example.                                        commit e31edd8

--------------------------------------------------------------------------------
SECTION 13 — MATCH MORE / RESILIENCE / FORMATS
--------------------------------------------------------------------------------
[DONE] E1  CJK/native-script norm_title + empty-key grouping guard.  commit 757281a
[DONE] E2/E4  Dependency-free fuzzy scorer + transparent mobi/azw3 import. 114cfbc
[DONE] E3  Anti-bot block ledger + escalating host cooldown.         commit 682895a
[DONE] 13A  Poison-proof AniList match, edition-quality ranking, ComicInfo.xml,
       persist identity on taxonomy-less match, OL alt-title ratio gate; structured
       Prowlarr type=book/ISBN search + concurrent multi-provider field-merge.
                                            commits f3eb1f2, cbe35c3, 4604706
[DONE] 13B  SAB stall detection + auto-escalate plain-HTTP challenge to render; reuse
       cf_clearance (persist Playwright storage_state, copy into the httpx jar) +
       per-host fairness semaphore + circuit breaker.       commits 73ec9ab, 7efe1ef
[DONE] 13C  Encoding detection (no mojibake), single-doc EPUB + bookmark-less PDF
       chapterization, scanned-PDF -> image gallery (PyMuPDF), content-hash import
       dedupe (works.content_hash + alembic 0025).   commits ae2a07c, f98611a, 42c30a2

--------------------------------------------------------------------------------
SECTION 14 — DEDUP THE INDEX + PERSIST MATCHES
--------------------------------------------------------------------------------
[DONE] K1/14A/14B  Persisted identity_key, identity-based merge, stable group id,
       fetch-by-id re-enrich, negative cache for transient-failed rows, per-volume
       title collapse via explicit volume markers, rarest-token blocking.
                          commits ac88167, fbf79d0, a0777cd, f39f381, e18b7d2
[DONE] 14B  AniList release-watch batched via fetch_many (N -> ~N/50 GraphQL calls);
       series enumeration persisted to extra["series_members"] (survives restart).
                                                          commits 77f7d06, 21a6c05
[DONE*] 14A series-card collapse  Folding distinct-titled volumes into one CatalogGroup
       would break acquire (it treats every group member as a SOURCE of one work).
       Shipped as a PRESENTATION-only fold instead: catalog.collapse_series_cards merges
       same-series volume cards in the browse into the most-popular one (re-titled to
       the series + series_count badge), leaving the work-grouping + acquire untouched;
       each volume stays acquirable via search + View Series. Browse-only, not search.
                                                                     commit df0bb8f

--------------------------------------------------------------------------------
SECTION (UX) — FRONTEND
--------------------------------------------------------------------------------
[DONE] UX1-UX4,UX6  Load-failure states, NaN-safe Sources, reader restore + comic
       stale-closure fixes.                                         commit bb3514e
[DONE] UX5  Destructive confirms (useConfirm): delete download + delete crawl job
       (Jobs), unmap watched folder (AddWork), grant/revoke admin + disable/enable
       user (Users).                                                 commit 911becc


================================================================================
PART B — END-OF-PROGRAM SWEEP
================================================================================
(Populated below after Part A. Two passes: B1 static codebase sweep, B2 visual /
runtime sweep via Playwright against the running instance.)

--- B1. CODEBASE SWEEP -------------------------------------------------------
(pending — see below)

--- B2. PLAYWRIGHT / RUNTIME SWEEP -------------------------------------------
(pending — see below)
